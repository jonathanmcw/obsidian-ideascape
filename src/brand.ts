// The plugin's user-visible name and id, in one place. The id is also the view type and the frontmatter key new
// maps are written with (see MAP_FORMATS in organiser/model/markdown.ts; a test keeps them equal to manifest.json).
// Maps written before the plugin had its own name say `idea-map` and `%%ideamap`, and open the same.
export const PLUGIN_NAME = "Ideascape";
export const PLUGIN_ID = "ideascape";
